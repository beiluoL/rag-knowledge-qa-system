# RAG 企业级知识库问答系统 — 项目说明

> 📖 本文档面向非技术背景读者，力求用通俗易懂的语言解释每一个技术概念。
> 无论你是毕设答辩、项目交接、还是自己学习，这份文档都能帮你彻底理解这个项目。

---

## 目录

1. [项目概述](#1-项目概述)
2. [环境要求](#2-环境要求)
3. [环境部署（超详细）](#3-环境部署超详细)
4. [如何运行项目](#4-如何运行项目)
5. [技术框架详解](#5-技术框架详解)
6. [RAG 实现原理](#6-rag-实现原理)
7. [技术选型对比](#7-技术选型对比)
8. [数据库设计详解](#8-数据库设计详解)
9. [API 接口详解](#9-api-接口详解)
10. [前端页面说明](#10-前端页面说明)
11. [离线 vs 在线模式](#11-离线-vs-在线模式)
12. [安全设计](#12-安全设计)
13. [性能优化策略](#13-性能优化策略)
14. [常见问题排查](#14-常见问题排查)
15. [扩展开发指南](#15-扩展开发指南)

---

## 1. 项目概述

### 1.1 这个项目是做什么的？

这是一个**企业级知识库智能问答系统**。通俗地说：

> 管理员上传商品相关的文档（比如产品手册、规格说明书、常见问题 FAQ 等），系统「读懂」这些文档后，用户就可以在聊天界面用自然语言提问，AI 会自动从文档中找到答案并生成回复。

**举个例子：**
- 管理员上传了《iPhone 15 产品手册.pdf》
- 用户问："iPhone 15 的芯片是什么？"
- AI 回答："根据资料 [1]，iPhone 15 采用 A16 仿生芯片……"
- 用户点击 [1] 就能看到信息来源

### 1.2 什么是 RAG？

**RAG** 是 Retrieval-Augmented Generation 的缩写，中文叫「检索增强生成」。

你可以把 RAG 理解为一个**带着参考书考试的学霸**：

| 对比 | 普通 AI（如 ChatGPT） | RAG 系统（本项目） |
|------|----------------------|-------------------|
| 知识来源 | 训练时学到的知识（可能过时） | 你上传的文档（实时更新） |
| 回答准确性 | 可能「一本正经地胡说八道」 | 基于文档回答，可追溯来源 |
| 数据安全 | 数据上传到云端 | 可完全本地运行 |
| 适用场景 | 通用聊天 | 企业专属知识库 |

**RAG 的两个核心步骤：**

1. **R（检索 Retrieval）**：从知识库中搜索与用户问题最相关的文档片段
2. **G（生成 Generation）**：把搜到的资料和用户问题一起交给 AI，让 AI 基于资料生成回答

### 1.3 系统工作流程（一句话版）

```
上传文档 → 解析文本 → 切成小块 → 转成数学向量 → 存入数据库
                                                      ↓
用户提问 → 问题转向量 → 搜索相似向量 → 拼接提示词 → AI 生成回答 → 打字机展示
```

---

## 2. 环境要求

### 2.1 硬件要求

| 项目 | 最低要求 | 推荐配置 | 本项目使用 |
|------|----------|----------|-----------|
| CPU | 4 核 | 8 核 | Apple M1 (8 核) |
| 内存 | 8GB | 16GB+ | 16GB 统一内存 |
| 硬盘 | 20GB 可用 | 50GB+ SSD | — |
| 网络 | 仅在线模式需要 | — | 离线模式无需联网 |

### 2.2 软件要求

| 软件 | 版本 | 用途 | 必须？ |
|------|------|------|--------|
| Java JDK | 17 或以上 | 运行后端服务 | ✅ 必须 |
| Maven | 3.8 或以上 | 后端项目构建 | ✅ 必须 |
| Node.js | 20 或以上 | 运行前端项目 | ✅ 必须 |
| PostgreSQL | 17 | 数据库和向量存储 | ✅ 必须 |
| pgvector | 0.8.4 | 向量搜索扩展 | ✅ 必须 |
| Ollama | 0.30+ | 本地 AI 模型运行 | 仅离线模式 |
| Git | 2.0+ | 代码版本管理 | 可选 |

---

## 3. 环境部署（超详细）

本节会**手把手**带你完成从零到运行的所有环境搭建。如果你用的是 Mac，直接照着做即可。Windows/Linux 用户请参考对应系统的安装命令。

### 3.1 安装 Java 17

**它是什么？** Java 是这个项目后端使用的编程语言，JDK 是 Java 的开发工具包。你可以把它理解为「运行后端代码所需要的翻译器」。

```bash
# Mac 使用 Homebrew 安装
brew install openjdk@17

# 配置环境变量（把下面这行加到 ~/.zshrc 文件末尾）
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# 验证安装
java --version
# 应该看到类似: openjdk 17.0.x ...
```

> **Windows 用户**：去 [Oracle JDK 17](https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html) 或 [Eclipse Temurin](https://adoptium.net/) 下载安装包，安装后配置 `JAVA_HOME` 环境变量。

### 3.2 安装 Maven

**它是什么？** Maven 是 Java 项目的「管家」—— 它能自动下载项目需要的第三方代码库（jar 包），并把代码编译成可运行的程序。类比：你去餐厅吃饭，Maven 就是帮你买菜、配菜的厨师长。

```bash
# Mac
brew install maven

# 验证安装
mvn --version
# 应该看到: Apache Maven 3.9.x 和 Java 17
```

### 3.3 安装 Node.js

**它是什么？** Node.js 让 JavaScript 可以在浏览器之外运行。我们的前端项目（Vue 3）需要 Node.js 来编译和运行。

```bash
# Mac
brew install node@20

# 验证安装
node --version  # 应该看到 v20.x.x
npm --version   # npm 是 Node 的包管理器
```

### 3.4 安装 PostgreSQL 和 pgvector

**PostgreSQL 是什么？** 一个免费开源的关系型数据库，简单理解就是一个「超强的 Excel 表格」，用来存储用户信息、文档记录、聊天消息等。

**pgvector 是什么？** PostgreSQL 的一个扩展插件，让数据库能存储和搜索「向量」（一串数字）。向量搜索是实现「找到和用户问题最相关的文档」的关键技术。

```bash
# Mac 安装
brew install postgresql@17 pgvector

# 启动 PostgreSQL 服务（设为开机自启）
brew services start postgresql@17

# 创建数据库和用户
# 注意：如果你的 Mac 用户名不是 beiluo，把 $(whoami) 换成你的用户名
psql -U $(whoami) -d postgres <<EOF
-- 创建数据库用户
CREATE USER ragkb WITH PASSWORD 'ragkb123';

-- 创建数据库
CREATE DATABASE ragkb OWNER ragkb;

-- 赋予权限
GRANT ALL PRIVILEGES ON DATABASE ragkb TO ragkb;
EOF

# 启用 pgvector 扩展
psql -U $(whoami) -d ragkb -c "CREATE EXTENSION IF NOT EXISTS vector;"

# 验证
psql -U ragkb -d ragkb -c "\dx"
# 应该看到 vector 扩展在列表中
```

> **Windows 用户**：去 [PostgreSQL 官网](https://www.postgresql.org/download/windows/) 下载安装包，安装时记得勾选「Stack Builder」，然后通过 Stack Builder 安装 pgvector 扩展。

### 3.5 安装 Ollama 和下载模型（离线模式）

**Ollama 是什么？** 一个让你在本地电脑上运行大语言模型（LLM）的工具。它就像一个「本地版的 ChatGPT」，不需要联网，数据不会离开你的电脑。

**模型是什么？** 大语言模型就是一个「超级大脑」，它能理解自然语言并生成回答。我们使用两个模型：
- **对话模型（Qwen2.5:7b）**：负责「思考」和「生成回答」，类似 ChatGPT 的大脑
- **向量模型（nomic-embed-text）**：负责「理解语义」，把文字转成数学向量，用于搜索

```bash
# 安装 Ollama
brew install ollama

# 启动 Ollama 服务
ollama serve &

# 下载对话模型（约 4.7GB，首次下载需要时间）
ollama pull qwen2.5:7b

# 下载向量模型（约 1.2GB）
ollama pull nomic-embed-text

# 验证模型已下载
ollama list
# 应该看到 qwen2.5:7b 和 nomic-embed-text 在列表中
```

> **注意**：如果你的网络下载 Ollama 模型很慢，可能是被墙了。可以配置镜像源或使用代理。在 macOS 上，Ollama 默认从 `registry.ollama.ai` 下载，你可以在 `~/.ollama/config` 里配置镜像。

### 3.6 配置项目私密信息

```bash
# 进入项目目录
cd rag-knowledge-qa-system

# 复制私密配置模板
cp backend/src/main/resources/application-secrets.example.yml \
   backend/src/main/resources/application-secrets.yml

# 编辑私密配置文件
# 如果你只使用离线模式，只需要修改数据库密码
# 如果想使用在线模式，还需要填写阿里云百炼的 API Key
nano backend/src/main/resources/application-secrets.yml
```

`application-secrets.yml` 文件内容说明：

```yaml
spring:
  datasource:
    password: ragkb123          # 改成你自己设置的数据库密码

app:
  dashscope:
    api-key: sk-xxxx            # 在线模式需要，去 https://dashscope.aliyun.com/ 申请

  jwt:
    secret: 你的JWT密钥          # 用于加密用户登录令牌，可以自己随便写一串长字符
```

---

## 4. 如何运行项目

### 4.1 启动顺序

需要按顺序启动 **4 个服务**（离线模式）：

| 步骤 | 服务 | 端口 | 说明 |
|------|------|------|------|
| 1 | PostgreSQL | 5432 | 数据库 |
| 2 | Ollama | 11434 | AI 模型（仅离线模式） |
| 3 | Spring Boot 后端 | 8080 | 业务服务 |
| 4 | Vue 3 前端 | 5173 | 用户界面 |

### 4.2 逐步启动

```bash
# ─── 步骤 1：确认 PostgreSQL 在运行 ───
brew services list | grep postgresql
# 如果没有 running，执行: brew services start postgresql@17

# ─── 步骤 2：启动 Ollama（仅离线模式） ───
# 打开一个新终端窗口
ollama serve
# 保持这个窗口不要关

# ─── 步骤 3：启动后端 ───
# 打开另一个新终端窗口
cd rag-knowledge-qa-system/backend
mvn spring-boot:run
# 看到 "Started RagKbApplication in X seconds" 表示启动成功

# ─── 步骤 4：启动前端 ───
# 打开再一个新终端窗口
cd rag-knowledge-qa-system/frontend
npm install        # 第一次需要，之后不需要
npm run dev
# 看到 "Local: http://localhost:5173/" 表示启动成功

# ─── 步骤 5：打开浏览器 ───
open http://localhost:5173
```

### 4.3 默认账号

| 角色 | 用户名 | 密码 | 有什么权限 |
|------|--------|------|-----------|
| 管理员 | admin | 123456 | 上传文档、管理知识库、问答 |
| 普通用户 | 自己注册 | 自行设置 | 问答、管理自己的会话 |

### 4.4 首次使用流程

1. **注册/登录**：在浏览器中打开 `http://localhost:5173`，使用 `admin` / `123456` 登录
2. **上传文档**：点击左侧「知识库管理」→ 上传一个 PDF 或 TXT 文件
3. **等待处理**：文档上传后，系统会自动解析、切分、向量化（一般几秒到几十秒，取决于文档大小）
4. **开始提问**：回到「问答」页面，新建会话，开始提问

### 4.5 切换离线/在线模式

前端界面左上角有一个模式指示灯：
- 🟢 **离线模式**：使用本地 Ollama 模型，完全离线免费
- 🔵 **在线模式**：使用阿里云百炼 API，需要配置 API Key，效果更强

点击指示灯即可实时切换，不需要重启服务。

---

## 5. 技术框架详解

### 5.1 整体架构一览

```
┌─────────────────────────────────────────┐
│               浏览器 (Chrome/Safari)       │
│         Vue 3 + Element Plus UI          │
│              http://localhost:5173       │
└────────────────┬────────────────────────┘
                 │ HTTP 请求 + JWT Token
                 ▼
┌─────────────────────────────────────────┐
│          Spring Boot 3 后端服务            │
│            http://localhost:8080          │
│                                          │
│  ┌──────────────────────────────────┐    │
│  │     Spring Security (安全守卫)     │    │
│  │   JWT Filter → 验证身份 → 放行/拦截  │    │
│  └──────────────────────────────────┘    │
│  ┌──────────────────────────────────┐    │
│  │        Controller 层 (接待员)       │    │
│  │  接收HTTP请求 → 参数校验 → 调用Service │    │
│  └──────────────────────────────────┘    │
│  ┌──────────────────────────────────┐    │
│  │         Service 层 (业务逻辑)       │    │
│  │  RAG编排 → 文本切分 → 向量化 → 搜索   │    │
│  └──────────────────────────────────┘    │
│  ┌──────────────────────────────────┐    │
│  │       Repository 层 (数据访问)      │    │
│  │     JPA + 原生SQL向量搜索          │    │
│  └──────────────────────────────────┘    │
└──────┬──────────────────────┬────────────┘
       │ JDBC 数据库连接        │ HTTP 调用
       ▼                      ▼
┌──────────────┐   ┌──────────────────┐
│ PostgreSQL   │   │  Ollama (离线)    │
│ + pgvector   │   │  或 DashScope    │
│ :5432        │   │  (在线)          │
└──────────────┘   └──────────────────┘
```

### 5.2 后端框架：Spring Boot 3

**它是什么？** Spring Boot 是 Java 世界中最流行的后端开发框架。它就像一个「乐高底版」，你只要在上面搭积木（加依赖），就能快速搭建一个 Web 服务。

**通俗理解：** 如果你要开一家餐厅：
- **Spring Boot** = 已经装修好的店铺 + 厨房设备，你只需要请厨师、写菜单
- **不用 Spring Boot** = 从毛坯房开始，自己砌墙、拉电线、装水管……

**本项目用了 Spring Boot 的哪些能力？**

| Spring 模块 | 做了什么 |
|-------------|---------|
| Spring Web (MVC) | 处理 HTTP 请求，提供 REST API |
| Spring Security | 用户认证（登录）+ 权限控制（管理员/普通用户） |
| Spring AI | 统一调用 AI 模型的接口（离线 Ollama + 在线 DashScope） |
| Spring Data JPA | 操作数据库（增删改查不用写 SQL） |
| Flyway | 自动创建/更新数据库表结构 |

### 5.3 前端框架：Vue 3 + Element Plus

**Vue 3 是什么？** 一个构建用户界面的 JavaScript 框架。它采用「组件化」思想，把页面拆成一个个独立的小部件（组件），每个组件负责自己的显示和交互。

**Element Plus 是什么？** 一套基于 Vue 3 的 UI 组件库。它提供了现成的按钮、表单、表格、对话框等组件，不用自己从零写样式。

**通俗理解：**
- **Vue 3** = 乐高积木的连接规则，告诉你哪些积木可以拼在一起
- **Element Plus** = 已经做好的积木块（带颜色、形状），你直接拿来用
- 不用 Element Plus = 每块积木都要自己雕刻、上色

**前端核心依赖：**

| 库 | 用途 |
|----|------|
| Vue 3 | 前端框架 |
| Element Plus | UI 组件库（按钮/表单/表格/对话框） |
| Vue Router | 页面路由（URL 和页面之间的映射） |
| Pinia | 全局状态管理（在多个组件间共享数据） |
| Axios | HTTP 请求工具（前端和后端通信） |
| marked | Markdown 文本转 HTML（让 AI 回答可以带格式） |
| highlight.js | 代码语法高亮 |
| Vite | 前端构建工具（快速编译、热更新） |

### 5.4 数据库：PostgreSQL + pgvector

**PostgreSQL（简称 PG）：** 世界上最先进的开源关系型数据库之一。它是「关系型数据库」，用表格来组织数据（类似 Excel 的多个 Sheet）。

**pgvector 扩展：** 让 PostgreSQL 能够存储和搜索「向量」——这是 RAG 系统的核心能力。

**数据库 vs 普通文件存储的对比：**

| 对比维度 | 存文件（如 JSON/TXT） | 用数据库（PostgreSQL） |
|----------|---------------------|----------------------|
| 查询速度 | 文件大时很慢 | 有索引，非常快 |
| 并发访问 | 多人同时操作容易出错 | 事务保证数据一致 |
| 向量搜索 | 不支持 | pgvector 支持余弦相似度搜索 |
| 数据安全 | 容易丢失 | 有备份和恢复机制 |

### 5.5 AI 模型：Ollama + Qwen2.5

**什么是大语言模型（LLM）？**

大语言模型就像一个「读过海量书籍的学生」。它通过阅读互联网上的大量文本，学会了理解语言、回答问题、写文章。你问它一个问题，它能根据学到的知识生成回答。

**Qwen2.5:7b（通义千问）：**
- Qwen = 阿里云开发的「通义千问」模型系列
- 2.5 = 版本号
- 7b = 70 亿个参数（参数越多，模型越大，能力越强，但也越吃内存）
- 特别擅长中文，适合中文通用与行业知识场景

**bge-m3（智源嵌入模型）：**
- 由北京智源人工智能研究院开发
- 它的任务不是「回答问题」，而是「理解语义」
- 把一段文字转换成一串数字（向量），语义相似的文字，向量也相似
- 输出 1024 维向量（1024 个浮点数）

**Embedding（向量嵌入）是什么？**

想象你面前有一张地图：
- 相似的城市在地图上靠得近，不相干的城市离得远
- 「向量嵌入」就是把文字映射到这样一个「语义地图」上
- 「iPhone 15 芯片」和「A16 处理器」在语义地图上靠得很近
- 「iPhone 15 芯片」和「今天天气不错」在语义地图上离得很远

这就是 pgvector 能实现「语义搜索」的数学基础。

### 5.6 文档解析：Apache Tika

**它是什么？** Apache Tika 是一个「万能文档解析器」。它能从各种格式的文件中提取纯文本内容。

**支持的格式：**
- PDF（.pdf）
- Word（.docx, .doc）
- Excel（.xlsx, .xls）
- Markdown（.md）
- 纯文本（.txt）
- HTML（.html）
- PPT（.pptx）
- 等等……

**通俗理解：** Tika 就像一个翻译官，不管你说中文、英文、日语，他都能翻译成一种通用语言。不管你的文档是 PDF 还是 Word，Tika 都能取出里面的纯文本。

---

## 6. RAG 实现原理

### 6.1 整体流程

RAG 分为两个阶段：**文档入库（Ingestion）** 和 **问答（Query）**。

### 6.2 文档入库流程（6 步）

```
┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│ 上传文档  │ → │ Tika解析  │ → │ 文本切分  │ → │ 向量化   │ → │ 存储向量  │ → │ 更新状态  │
│ (前端)   │   │ 提取文本  │   │ 500字/块  │   │ 768维向量 │   │ pgvector  │   │ COMPLETED│
└──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
```

#### 步骤 1：文件上传（POST /api/knowledge/documents/upload）

- 用户在前端上传文件
- 后端校验：文件类型是否在白名单（pdf/txt/md/docx/xlsx/csv），大小不超过 20MB
- 文件保存到 `./data/documents/` 目录
- 在数据库中创建文档记录，状态设为 `PENDING`（待处理）

#### 步骤 2：文本解析（Apache Tika）

- 使用 `AutoDetectParser` 自动检测文件类型
- `BodyContentHandler` 提取文本正文（限制 -1 表示不限制长度）
- 解析完成后，状态更新为 `PROCESSING`（处理中）

#### 步骤 3：文本切分（TextSplitter）

这一步非常重要！为什么要切分？

- LLM 一次能处理的文本长度有限（大约 4000-8000 字）
- 切分后可以在搜索时精准定位到最相关的段落
- 太长会稀释语义，太短会丢失上下文

**切分策略（中文友好）：**

| 参数 | 值 | 说明 |
|------|-----|------|
| chunk_size | 500 字符 | 每块最多 500 个字符 |
| chunk_overlap | 100 字符 | 相邻两块之间重叠 100 个字符 |

```
原文: "iPhone 15 采用 A16 仿生芯片，性能相比上一代提升 30%。屏幕为 6.1 英寸 OLED..."

分块 1: "iPhone 15 采用 A16 仿生芯片，性能相比上一代提升 30%。屏幕为 6.1 英寸 OLED..."
分块 2: "                 ...屏幕为 6.1 英寸 OLED，支持灵动岛功能..."  (100字符重叠)
```

**为什么要有重叠？**
- 防止关键信息刚好被切在边界处而导致上下文丢失
- 比如「iPhone 15 的芯片是」在一块的末尾，「A16 仿生芯片」在下一块的开头

**切分算法：**
- 优先在段落边界处切（`\n` 换行符）
- 其次在句号、问号、感叹号处切
- 如果还是超长，在 `chunk_size` 处硬切

#### 步骤 4：向量化（Embedding）

- 把每块文本发给 Ollama 的 bge-m3 模型（或 DashScope 的 text-embedding-v2）
- 模型返回一个浮点数数组（向量）
- 离线模式：768 维（nomic-embed-text）
- 在线模式：1536 维（text-embedding-v2）
- 批量处理：16 条/批，减少网络往返次数

#### 步骤 5：向量存储

- 把向量以二进制格式存入 PostgreSQL 的 `chunk_embeddings` 表
- pgvector 的 `vector` 类型支持高效的向量运算

#### 步骤 6：更新状态

- 文档状态更新为 `COMPLETED`
- 记录分块数量 `chunk_count`
- 如果任何步骤出错，状态更新为 `FAILED`

### 6.3 问答流程（6 步）

```
┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│ 接收问题  │ → │ 问题向量化 │ → │ 语义搜索  │ → │ Prompt拼接 │ → │ AI 生成  │ → │ SSE流式  │
│ 保存消息  │   │ 768/1536维│   │ Top-5    │   │ System+检索 │   │ 逐字生成  │   │ 前端展示  │
└──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
```

#### 步骤 1：接收用户问题

- 后端收到 POST `/api/chat/send` 请求
- 保存用户消息到 messages 表
- 如果这是新会话的第一条消息，自动创建会话
- 会话标题自动截取问题前 30 个字

#### 步骤 2：问题向量化

- 把用户问题发送给向量模型
- 模型返回一个与文档分块同维度的向量

#### 步骤 3：语义搜索（核心步骤）

- 使用 pgvector 的余弦相似度运算 `<=>`（cosine distance operator）
- 在向量表中找到与问题向量最相似的 Top-5 个分块
- 过滤掉相似度低于 0.5 的低相关结果

**SQL 查询的核心语法：**
```sql
SELECT c.content, d.title,
       1 - (ce.embedding <=> '问题向量') AS similarity
FROM chunk_embeddings ce
JOIN chunks c ON c.id = ce.chunk_id
JOIN documents d ON d.id = c.document_id
WHERE 1 - (ce.embedding <=> '问题向量') >= 0.5    -- 过滤相似度 < 0.5
ORDER BY ce.embedding <=> '问题向量'               -- 按距离排序
LIMIT 5;                                           -- 只取前5条
```

**什么是余弦相似度？**

想象空间中有两个箭头从原点出发：
- 两个箭头方向越接近，夹角越小，余弦相似度越接近 1.0
- 两个箭头方向垂直（90°），余弦相似度为 0
- 两个箭头方向相反（180°），余弦相似度为 -1.0

对于文本向量来说：
- 相似度 0.95 = 两段文字语义高度相关
- 相似度 0.5 = 有一定的相关性
- 相似度 0.1 = 基本不相关

#### 步骤 4：Prompt 拼接

把四部分内容拼接成发给 AI 的完整消息：

```
┌──────────────────────────────────────────────┐
│ System Prompt（系统提示词）                     │
│ "你是知识库助手，请严格基于参考资料回答..."     │
├──────────────────────────────────────────────┤
│ 【参考资料】  (语义搜索的 Top-5 结果)            │
│ [1] 来源: iPhone 15 产品手册.pdf               │
│     iPhone 15 采用 A16 仿生芯片...             │
│ [2] 来源: iPhone 15 规格表.xlsx                │
│     A16 Bionic chip, 6-core CPU...           │
│ ...                                          │
├──────────────────────────────────────────────┤
│ 【历史对话】  (最近 3 轮，200 字/轮)             │
│ 用户: iPhone 15 有哪些颜色？                    │
│ 助手: 根据资料，iPhone 15 有粉色、黄色...        │
├──────────────────────────────────────────────┤
│ 【用户问题】                                    │
│ iPhone 15 的芯片是什么？                        │
└──────────────────────────────────────────────┘
```

**System Prompt 的作用：**
- 定义 AI 的角色（知识库助手）
- 约束 AI 的行为（只能基于参考资料回答）
- 防止 AI 胡说八道（明确告知未找到信息时的处理方式）
- 标注引用格式（用 [编号] 标注来源）

#### 步骤 5：AI 生成回答

- 调用 Ollama 的 Qwen2.5:7b 模型（或 DashScope 的 qwen-plus）
- 参数设置：
  - `temperature=0.3`（回答越确定，减少随机性）
  - `num_predict=1024`（最多生成 1024 个 token）
- AI 逐字生成回答

**temperature（温度）参数解释：**
- temperature = 0：每次回答都一样（最确定，适合数学计算）
- temperature = 0.3：回答比较稳定（知识问答推荐值）
- temperature = 0.7：回答有变化（适合创意写作）
- temperature = 1.0+：回答天马行空（容易胡说八道）

#### 步骤 6：SSE 流式推送

**SSE（Server-Sent Events）是什么？**

传统的 HTTP 请求就像寄信——你发一封信，等对方回信，一次性收到完整回复。

SSE 就像打电话——接通后，对方可以持续说话，你实时听到，不需要挂断再打。

**SSE 事件格式：**
```
event: conversation
data: {"conversationId": 12}

event: status
data: 正在搜索知识库...

event: references
data: [{"documentId":1, "documentTitle":"iPhone 15 产品手册.pdf", "score":0.923}, ...]

event: content
data: 根据参考资料[1]，iPhone 15 采用的是 A16 仿生芯片。

event: done
data: {"conversationId":12, "references":[...]}
```

每种事件类型的作用：
- `conversation`：告诉前端会话 ID，用于后续加载历史
- `status`：显示处理状态（"正在搜索..."）
- `references`：发送引用来源列表
- `content`：AI 回答的文本（逐字/逐句推送，实现打字机效果）
- `done`：回答完成，包含完整引用信息

**前端怎么解析 SSE？**

```javascript
// 使用 Fetch API + ReadableStream
const response = await fetch('/api/chat/send', { method: 'POST', body: ... });
const reader = response.body.getReader();

while (true) {
  const { done, value } = await reader.read();
  if (done) break;

  // 将收到的字节解码为文本
  const text = new TextDecoder().decode(value);

  // 按行解析 SSE 格式
  // 追踪 event: 行来确定当前事件类型
  // 累积跨网络包的数据
}
```

**这个实现中解决的几个 SSE 坑：**
1. 网络包可能把一个 SSE 事件切成两半发送 → 用 buffer 累积数据
2. `event: content` 的数据是纯文本（不是 JSON）→ 按事件类型区分处理
3. Vue Router 的 `router.replace()` 会中断 fetch 连接 → 改用 `history.replaceState()`

---

## 7. 技术选型对比

### 7.1 后端框架对比

| 对比维度 | Spring Boot 3 ✅ (已选) | Django (Python) | Node.js + Express |
|----------|------------------------|-----------------|-------------------|
| 语言 | Java | Python | JavaScript |
| 学习曲线 | 中等 | 较简单 | 简单 |
| 性能 | ⭐⭐⭐⭐⭐ 高 | ⭐⭐⭐ 中等 | ⭐⭐⭐ 中等 |
| AI/ML 生态 | Spring AI（新兴） | LangChain, LlamaIndex（成熟） | LangChain.js |
| 数据库集成 | JPA/Hibernate（强大） | Django ORM（方便） | Prisma/Knex（灵活） |
| 企业级特性 | Security, 事务, 连接池（完善） | 需插件 | 需中间件 |
| 适合场景 | 企业级后端系统 | 快速原型 + AI 应用 | 轻量 API 服务 |
| **选择理由** | 用户熟悉 Java，生态完善 | — | — |

### 7.2 前端框架对比

| 对比维度 | Vue 3 ✅ (已选) | React 18 | Angular |
|----------|-----------------|----------|---------|
| 学习曲线 | ⭐⭐ 较简单 | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐⭐ 陡峭 |
| 组件库 | Element Plus（企业级） | Ant Design, MUI | Angular Material |
| 响应式 | 自动（ref/reactive） | 手动（useState） | RxJS（复杂） |
| 包体积 | 小 | 中 | 大 |
| 国内流行度 | 非常高 | 高 | 低 |
| **选择理由** | 用户熟悉，上手快 | — | — |

### 7.3 AI 模型对比

| 对比维度 | Qwen2.5:7b ✅ (离线) | Llama 3.1:8b | Mistral 7b | ChatGPT (云端) |
|----------|---------------------|-------------|------------|----------------|
| 中文能力 | ⭐⭐⭐⭐⭐ 优秀 | ⭐⭐⭐ 还行 | ⭐⭐ 较差 | ⭐⭐⭐⭐ 良好 |
| 硬件需求 | ~5GB 显存/内存 | ~5GB | ~4GB | 不需要 |
| 需要联网 | ❌ 不需要 | ❌ 不需要 | ❌ 不需要 | ✅ 需要 |
| 费用 | 免费 | 免费 | 免费 | 按 token 计费 |
| 数据隐私 | ⭐⭐⭐⭐⭐ 本地 | ⭐⭐⭐⭐⭐ 本地 | ⭐⭐⭐⭐⭐ 本地 | ⭐⭐ 数据上云 |
| **选择理由** | 中文最好，模型已在本地 | — | — | — |

### 7.4 向量数据库对比

| 对比维度 | pgvector ✅ (已选) | Milvus | Chroma | FAISS (Meta) |
|----------|-------------------|--------|--------|-------------|
| 类型 | PostgreSQL 扩展 | 独立数据库 | 独立数据库 | 向量索引库 |
| 部署难度 | ⭐ 简单（同一个 PG） | ⭐⭐⭐⭐ 复杂 | ⭐⭐ 较简单 | ⭐⭐ 较简单 |
| 搜索速度 | 中等（百万级够用） | 极快（亿级） | 快 | 极快 |
| 过滤能力 | ⭐⭐⭐⭐⭐ SQL 任意过滤 | ⭐⭐⭐ 有限 | ⭐⭐ 弱 | ⭐ 基本没有 |
| 运维成本 | ⭐ 低（一个数据库） | ⭐⭐⭐⭐ 高 | ⭐⭐ 低 | ⭐⭐ 低 |
| 适合场景 | 中小规模（<1000万向量） | 大规模生产 | 原型 + 中小规模 | 纯搜索（无过滤） |
| **选择理由** | 与业务数据同库，部署运维简单 | — | — | — |

### 7.5 RAG 框架对比

| 对比维度 | 自实现 ✅ (已选) | LangChain | LlamaIndex | Spring AI |
|----------|-----------------|-----------|------------|-----------|
| 灵活性 | ⭐⭐⭐⭐⭐ 完全可控 | ⭐⭐⭐ 受限于框架 | ⭐⭐⭐ | ⭐⭐⭐ |
| 代码量 | 较多 | 较少 | 较少 | 中等 |
| 调试难度 | ⭐⭐ 逻辑透明 | ⭐⭐⭐⭐ 黑盒 | ⭐⭐⭐ | ⭐⭐⭐ |
| 学习成本 | 需要理解原理 | 需要学框架 | 需要学框架 | 需要学框架 |
| **选择理由** | 理解底层原理，毕设答辩有内容可讲 | — | — | — |

---

## 8. 数据库设计详解

### 8.1 ER 图（实体关系图）

```
users (用户表)
  │
  ├── 1:many ── documents (文档表) ── 1:many ── chunks (分块表) ── 1:1 ── chunk_embeddings (向量表)
  │
  └── 1:many ── conversations (会话表) ── 1:many ── messages (消息表)
```

### 8.2 各表详解

#### users（用户表）

存储系统用户信息。

| 字段 | 类型 | 说明 | 为什么这样设计 |
|------|------|------|---------------|
| id | BIGSERIAL | 自增主键 | 数据库自动生成，保证唯一 |
| username | VARCHAR(50) UNIQUE | 用户名，不可重复 | UNIQUE 约束防止重名 |
| password_hash | VARCHAR(255) | BCrypt 加密后的密码 | 绝不存储明文密码！ |
| email | VARCHAR(100) | 邮箱（可选） | 未来可用于找回密码 |
| role | VARCHAR(20) | ADMIN 或 USER | 权限控制的核心字段 |
| enabled | BOOLEAN | 是否启用 | 可以禁用用户而不删除数据 |
| created_at | TIMESTAMP | 创建时间 | 审计追踪 |
| updated_at | TIMESTAMP | 更新时间 | 审计追踪 |

**为什么密码要加密存储？**
即使数据库被泄露，攻击者也拿不到用户的实际密码。BCrypt 是单向加密——可以验证密码是否正确，但无法从加密结果反推出原密码。

#### documents（文档表）

存储上传到知识库的文档信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| title | VARCHAR(500) | 文档标题 |
| file_name | VARCHAR(500) | 原始文件名 |
| file_type | VARCHAR(50) | 文件类型（pdf/txt/md/docx） |
| file_path | VARCHAR(1000) | 服务器上的存储路径 |
| file_size | BIGINT | 文件大小（字节） |
| status | VARCHAR(20) | 处理状态：PENDING → PROCESSING → COMPLETED / FAILED |
| chunk_count | INTEGER | 分块数量 |
| uploaded_by | BIGINT FK→users.id | 谁上传的 |

**文档状态流转：**
```
PENDING (待处理)
  → PROCESSING (解析中)
    → COMPLETED (处理成功)
    → FAILED (处理失败)
```

#### chunks（分块表）

存储文档被切分后的每个文本块。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| document_id | BIGINT FK→documents.id | 属于哪个文档 |
| chunk_index | INTEGER | 序号（从 0 开始） |
| content | TEXT | 文本内容（最多 500 字符） |
| token_count | INTEGER | 估算的 token 数量 |

**CASCADE 删除：**
当删除一个文档时，它下面的所有 chunks 会自动删除（ON DELETE CASCADE）。同理，删除 chunk 时，对应的向量也会自动删除。

#### chunk_embeddings / chunk_embeddings_online（向量表）

这是 RAG 系统的核心表，存储文本的向量表示。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| chunk_id | BIGINT FK→chunks.id UNIQUE | 一对一关联分块 |
| embedding | vector(768) 或 vector(1536) | 向量数据 |
| created_at | TIMESTAMP | 创建时间 |

**为什么有两张向量表？**

离线模式（Ollama）和在线模式（DashScope）使用不同的向量模型，输出维度不同：
- Ollama nomic-embed-text：768 维
- DashScope text-embedding-v2：1536 维

pgvector 的 `vector(N)` 类型维度是固定的，不能动态改变。所以使用两张表分别存储不同维度的向量。

**运行时如何选择表？**

`DynamicAiProvider.getMode()` 返回当前模式（`offline` / `online`），`ChunkEmbeddingRepository.tableName()` 根据模式返回对应的表名。

#### conversations（会话表）

每次对话的容器。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| user_id | BIGINT FK→users.id | 谁的会话 |
| title | VARCHAR(200) | 标题（首问前 30 字） |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 最后活跃时间 |

**复合索引 `(user_id, updated_at DESC)`：**
优化「查询某个用户最近的会话列表」——这是最频繁的查询之一。

#### messages（消息表）

每条对话的具体内容。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| conversation_id | BIGINT FK→conversations.id | 属于哪个会话 |
| role | VARCHAR(20) | USER（用户） 或 ASSISTANT（AI） |
| content | TEXT | 消息内容 |
| references_data | TEXT | 引用来源（仅 AI 回复有） |

**references_data 格式：**
```json
[
  {
    "documentId": 1,
    "documentTitle": "iPhone 15 产品手册.pdf",
    "chunkId": 42,
    "contentSnippet": "iPhone 15 采用 A16 仿生芯片...",
    "score": 0.923
  }
]
```

使用 TEXT 类型存储 JSON 而不是 JSONB 类型，因为 Spring Data JPA 对 TEXT 类型的兼容性更好。

---

## 9. API 接口详解

### 9.1 认证机制

系统使用 **JWT（JSON Web Token）** 进行身份认证。

**JWT 是什么？**

JWT 就像一个「数字身份证」。你登录后，后端给你发一张电子身份证（token），之后每次访问后端，你只需要出示这张身份证，不需要反复输入密码。

**工作流程：**
```
1. 用户登录 → 后端验证密码 → 颁发 access_token（30分钟有效） + refresh_token（7天有效）
2. 前端存储 token 在 localStorage
3. 之后每次请求都在 HTTP Header 中带上: Authorization: Bearer <access_token>
4. JwtAuthFilter 拦截请求 → 验证 token 签名和有效期 → 放行或拒绝
5. 如果 access_token 过期 → 自动用 refresh_token 换新的 access_token
```

**为什么需要两个 Token？**

- access_token（短期 30 分钟）：就算被泄露，影响时间也有限
- refresh_token（长期 7 天）：存在前端，只用于刷新 access_token，不频繁传输

### 9.2 接口详细说明

#### 认证模块 `/api/auth`（无需登录）

**POST `/api/auth/login` — 用户登录**

```
请求:
{
  "username": "admin",
  "password": "123456"
}

响应:
{
  "accessToken": "eyJhbG...",
  "refreshToken": "eyJhbG...",
  "tokenType": "Bearer",
  "expiresIn": 1800,
  "user": {
    "id": 1,
    "username": "admin",
    "role": "ADMIN"
  }
}
```

**POST `/api/auth/register` — 用户注册**

```
请求:
{
  "username": "newuser",
  "password": "mypassword",
  "email": "user@example.com"      // 可选
}

响应: { "message": "注册成功" }
```

**POST `/api/auth/refresh` — 刷新 Token**

```
请求 Header: Authorization: Bearer <refresh_token>
请求体: 空

响应: 新的 accessToken + refreshToken
```

#### 知识库模块 `/api/knowledge`（需要 ADMIN）

**POST `/api/knowledge/documents/upload` — 上传文档**
- Content-Type: `multipart/form-data`
- 字段 `file`: 文件
- 限制：20MB，类型白名单（pdf/txt/md/docx/xlsx/csv）

**GET `/api/knowledge/documents` — 文档列表**
- 支持分页：`?page=0&size=10`

**DELETE `/api/knowledge/documents/{id}` — 删除文档**
- 级联删除：文档 → 分块 → 向量数据

**GET `/api/knowledge/stats` — 统计信息**
```json
{
  "documentCount": 15,
  "chunkCount": 3250,
  "embeddingCount": 3250
}
```

#### 问答模块 `/api/chat`（需要登录）

**POST `/api/chat/send` — 发送问题（SSE 流式返回）**

```
请求:
{
  "conversationId": null,    // null = 新建会话，传已有 ID = 继续对话
  "message": "iPhone 15 的芯片是什么？"
}

响应: SSE 流 (见 6.3 步骤 6)
```

**GET `/api/chat/conversations` — 会话列表**
- 返回当前用户的所有会话，按最后活跃时间倒序

**GET `/api/chat/conversations/{id}` — 获取会话消息**
- 返回该会话的所有消息（按时间顺序）

**PUT `/api/chat/conversations/{id}` — 重命名会话**

**DELETE `/api/chat/conversations/{id}` — 删除会话**
- 级联删除会话 → 消息

#### AI 模式模块 `/api/ai-mode`

**GET `/api/ai-mode` — 查询当前模式**
```json
{ "mode": "offline" }
```

**POST `/api/ai-mode/switch` — 切换模式**
```
请求: { "mode": "online" }
响应: { "mode": "online", "message": "已切换为在线模式" }
```

---

## 10. 前端页面说明

### 10.1 路由设计

| URL | 页面 | 谁可以访问 | 说明 |
|-----|------|-----------|------|
| `/login` | 登录页 | 所有人 | 包含登录表单 |
| `/register` | 注册页 | 所有人 | 含确认密码校验 |
| `/chat` | 问答主页 | 登录用户 | 空状态提示「新建会话」 |
| `/chat/:id` | 指定会话 | 登录用户 | 加载历史消息 |
| `/profile` | 个人中心 | 登录用户 | 查看信息 + 修改密码 |
| `/admin/knowledge` | 知识库管理 | 仅管理员 | 文档上传/列表/删除 |

### 10.2 核心组件

| 组件 | 功能 |
|------|------|
| ChatView | 主聊天界面：左侧会话列表 + 中间消息区 + 底部输入框 |
| ConversationList | 会话列表：新建、切换、删除、重命名 |
| MessageList | 消息展示：用户消息 + AI 回答（Markdown 渲染 + 引用标签） |
| ChatInput | 输入框：支持 Enter 发送 |
| ReferenceBadge | 引用来源标签：可点击查看详情 |
| AdminKnowledgeView | 知识库管理：上传对话框 + 文档表格 + 状态标签 |
| ModeIndicator | 模式指示灯：点击切换离线/在线 |

### 10.3 打字机效果实现

打字机效果的核心原理是 **SSE 流式接收 + 逐字追加**：

```javascript
// 1. 发送请求
const response = await fetch('/api/chat/send', {
  method: 'POST',
  headers: { 'Authorization': `Bearer ${token}` },
  body: JSON.stringify({ message: userInput })
});

// 2. 读取流
const reader = response.body.getReader();
let assistantMessage = '';

while (true) {
  const { done, value } = await reader.read();
  if (done) break;

  const text = new TextDecoder().decode(value);
  // 3. 解析 SSE 事件
  if (currentEvent === 'content') {
    assistantMessage += text;        // 追加新文字
    renderMarkdown(assistantMessage); // 重新渲染
    scrollToBottom();                // 自动滚到底部
  }
}
```

### 10.4 打字机光标动画

AI 正在生成回答时，消息末尾显示三个跳动的圆点（`.typing-dots`），通过 CSS 动画实现：

```css
.typing-dots span {
  animation: dot-bounce 1.4s infinite ease-in-out both;
}
.typing-dots span:nth-child(1) { animation-delay: -0.32s; }
.typing-dots span:nth-child(2) { animation-delay: -0.16s; }
```

---

## 11. 离线 vs 在线模式

### 11.1 两种模式对比

| 对比维度 | 离线模式 (Ollama) | 在线模式 (DashScope) |
|----------|------------------|---------------------|
| 需要联网 | ❌ 不需要 | ✅ 需要 |
| 费用 | 免费 | 按 token 计费（新用户有免费额度） |
| 回答质量 | 中等（7B 模型） | 高（qwen-plus 更大参数） |
| 回答速度 | 较慢（本地 CPU/GPU） | 较快（云端 GPU） |
| 数据隐私 | ⭐⭐⭐⭐⭐ 数据不出电脑 | ⭐⭐⭐ 数据上阿里云 |
| 向量维度 | 768 | 1536（更精细） |
| 稳定性 | 依赖本地硬件 | 依赖网络和云服务 |

### 11.2 技术实现：DynamicAiProvider

使用**策略模式**实现运行时切换：

```java
// 接口定义
interface AiProvider {
    String chat(String systemPrompt, String userMessage);
    float[] embed(String text);
    int getEmbeddingDimension();
    String getMode();
}

// 动态代理
class DynamicAiProvider implements AiProvider {
    private volatile String activeMode = "offline";  // volatile 保证多线程可见

    void switchMode(String mode) {
        this.activeMode = mode;
    }

    AiProvider current() {
        return "online".equals(activeMode)
            ? dashScopeProvider
            : ollamaProvider;  // 默认离线
    }
}
```

所有业务代码只依赖 `DynamicAiProvider`，不需要知道当前是离线还是在线模式。

### 11.3 双向量表设计

由于两种模式的向量维度不同，设计了两张向量表：

| 表 | 维度 | 模式 |
|----|------|------|
| chunk_embeddings | vector(768) | 离线 Ollama |
| chunk_embeddings_online | vector(1536) | 在线 DashScope |

**重要：** 两种模式的向量不互通。如果你在离线模式下上传了文档，切换到在线模式后需要重新上传，因为向量维度不匹配。

### 11.4 数据隔离注意事项

- 删除文档时，会**同时清理**两张向量表中的数据
- 统计向量总数时，是两张表数据的合计
- 切换模式不会自动迁移已有的向量数据

---

## 12. 安全设计

### 12.1 多层安全防护

| 层面 | 措施 | 防护什么 |
|------|------|---------|
| 传输层 | JWT 无状态认证 | 防止未登录用户访问 API |
| 权限层 | Spring Security 角色拦截 | 防止普通用户操作管理功能 |
| 密码 | BCrypt 加密存储 | 数据库泄露也不怕 |
| Token | 双 Token + 短期有效期 | Token 泄露影响可控 |
| 配置 | 私密文件 gitignore | API Key 不泄露到代码仓库 |
| CORS | 跨域白名单 | 防止恶意网站调用 API |
| 密码修改 | 需要验证旧密码 | 防止他人修改密码 |
| 前端路由守卫 | 未登录跳转登录页 | 防止非法访问页面 |

### 12.2 权限矩阵

| API 路径 | 匿名 | USER | ADMIN |
|----------|------|------|-------|
| `/api/auth/**` | ✅ | ✅ | ✅ |
| `/api/chat/**` | ❌ | ✅ | ✅ |
| `/api/user/**` | ❌ | ✅ | ✅ |
| `/api/ai-mode/**` | ❌ | ✅ | ✅ |
| `/api/knowledge/**` | ❌ | ❌ | ✅ |

### 12.3 敏感信息保护

```
项目中的敏感信息管理：

application.yml (公开，可提交 Git)
  ├── 使用 ${PLACEHOLDER} 占位符引用敏感值
  └── spring.config.import: optional:classpath:application-secrets.yml

application-secrets.yml (私密，gitignore 排除)
  ├── 数据库密码
  ├── DashScope API Key
  ├── JWT 签名密钥
  └── 管理员默认密码

application-secrets.example.yml (模板，可提交 Git)
  └── 占位符说明，供其他开发者参考
```

### 12.4 Git 提交安全与双远程同步

#### 12.4.1 .gitignore 必须忽略的文件/目录

除了上面的「私密配置文件」外，以下本地目录**绝不能提交**——它们不含项目代码，但会泄露你的本地环境和个人信息：

| 忽略规则 | 排除原因 | 何时加入 |
|----------|----------|----------|
| `.claude/` | 本地 AI 编码工具缓存与配置 | 初始就已忽略 |
| `.workbuddy/` | 本地 Agent 记忆（个人偏好、项目私记），泄露即个人信息外泄 | 后续为防泄漏补入 |
| `.qoder/` | 本地 IDE/AI 工具目录，含本地状态 | 后续为防泄漏补入 |

> 💡 这两条（`.workbuddy/`、`.qoder/`）是项目演进过程中发现缺口后补的规则。最初仓库只忽略了 `.claude/` 和 `.env`、`application-secrets.yml`，直到发现 `git add -A` 会把个人 Agent 记忆一并推送到公开的 Gitee/GitHub，才补齐。

**提交前自检清单（强烈建议每次都做）：**

1. `git add -A`
2. `git status` —— 确认 `.workbuddy/`、`.qoder/`、`.env`、`application-secrets.yml` 都不在待提交列表
3. `git commit -m "说明"`
4. `git push origin master`

> ⚠️ 即使 `.gitignore` 已覆盖，若某次用 `git add -f` 强制添加，仍能绕过忽略规则。所以**不要强制添加**上述文件。

#### 12.4.2 双远程同步（Gitee + GitHub）

仓库已配置 `origin` 双 push-url，一次推送同时到达两个平台：

```bash
# 日常推送：自动同步 Gitee + GitHub
git push origin master

# 仅推 GitHub（备用远程）
git push github master
```

| 远程 | 地址 | 用途 |
|------|------|------|
| `origin` (fetch) | Gitee：git@gitee.com:beiluol/rag-knowledge-qa-system.git | 主远程，拉取用 |
| `origin` (push) | Gitee + GitHub 双推 | 推送同时到两平台 |
| `github` | GitHub：git@github.com:beiluoL/rag-knowledge-qa-system.git | 独立备用，仅推 GitHub |

> 两个平台使用 **SSH 协议**提交，需在本机配置好对应 SSH Key。推送前请确认 Gitee、GitHub 远程均已就绪。

---

## 13. 性能优化策略

### 13.1 后端优化

| 优化项 | 做法 | 效果 |
|--------|------|------|
| 向量搜索 | pgvector IVFFlat 索引 | 搜索从 O(n) 降到 O(√n) |
| 批量向量化 | 16 条/批调用 Ollama | 减少网络往返 |
| 文档处理异步化 | @Async 线程池 | 上传后立即返回，不阻塞 |
| 连接池 | HikariCP max-pool-size=20 | 支持多并发 |
| JVM 堆内存 | -Xmx4g | 为 Ollama 留足内存 |
| 上下文精简 | 历史对话仅带 3 轮 | Prompt 总长控制在合理范围 |

### 13.2 前端优化

| 优化项 | 做法 | 效果 |
|--------|------|------|
| 路由懒加载 | `() => import('./views/xxx.vue')` | 首屏加载更快 |
| KeepAlive 缓存 | 组件缓存 | 切换会话不丢失状态 |
| Markdown 渲染缓存 | 仅对新内容解析 | 避免重复渲染 |
| Element Plus 按需引入 | 只引入用到的组件 | 减小打包体积 |

### 13.3 硬件适配（M1 + 16GB）

| 服务 | 内存占用 |
|------|---------|
| Ollama + Qwen2.5:7b | ~5GB |
| Ollama + 向量模型 | ~1.5GB |
| PostgreSQL | ~0.5GB |
| Spring Boot JVM | ~1GB |
| 浏览器 | ~2GB |
| **合计** | **~10GB** |

剩余约 6GB 用于操作系统和突发使用。

---

## 14. 常见问题排查

### 14.1 后端启动失败

**问题：`Connection refused` 或 `Cannot connect to database`**

检查 PostgreSQL 是否启动：
```bash
brew services list | grep postgresql
# 如果没有 running: brew services start postgresql@17
```

**问题：`FATAL: password authentication failed`**

检查 `application-secrets.yml` 中的数据库密码是否正确：
```bash
psql -U ragkb -d ragkb -c "SELECT 1;"
# 如果提示密码错误，密码不是 ragkb123，用正确的密码
```

**问题：`relation "xxx" does not exist`**

数据库表没有创建。检查 Flyway 迁移是否执行：
```bash
psql -U ragkb -d ragkb -c "SELECT * FROM flyway_schema_history;"
# 应该看到 V1__init_schema.sql 和 V2__online_embedding.sql
```

### 14.2 Ollama 模型问题

**问题：`model not found`**

确认模型已经下载：
```bash
ollama list
# 应该看到 qwen2.5:7b 或 qwen2.5-coder:7b
```

**问题：Ollama 下载模型很慢或失败**

Ollama 默认从 Cloudflare R2 下载，国内可能有网络问题。可以：
1. 配置代理
2. 使用镜像源

### 14.3 前端问题

**问题：`npm install` 失败**

- 检查 Node.js 版本：`node --version`（需要 v20+）
- 清除缓存：`npm cache clean --force`
- 使用国内镜像：`npm config set registry https://registry.npmmirror.com`

**问题：前端登录后立即跳回登录页**

- 检查后端是否启动（http://localhost:8080）
- 打开浏览器开发者工具（F12）→ Console → 查看错误信息
- 可能是 CORS 跨域问题（确认后端 SecurityConfig 配置正确）

### 14.4 问答功能问题

**问题：发送消息后无响应**

1. 确认知识库中已有文档且状态为 COMPLETED
2. 确认 Ollama 服务正在运行：`curl http://localhost:11434`
3. 查看后端日志（终端中 Spring Boot 的输出）

**问题：在线模式切换后无法使用**

1. 确认 `application-secrets.yml` 中已配置有效的 DashScope API Key
2. 查看后端日志是否有 `DashScope API 调用失败` 的错误

**问题：回答不准确或与文档无关**

- 检查文档是否已经被处理完成（status=COMPLETED）
- 相似度阈值可能太高（默认 0.5），相关性低的结果被过滤了
- 检查上传的文档内容是否与问题相关
- 检查 System Prompt 中的约束是否生效

### 14.5 数据库问题

**问题：pgvector 扩展不存在**

```bash
psql -U ragkb -d ragkb -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

**问题：想清空数据重新开始**

```bash
# 重建数据库（会丢失所有数据！）
psql -U $(whoami) -d postgres -c "DROP DATABASE ragkb;"
psql -U $(whoami) -d postgres -c "CREATE DATABASE ragkb OWNER ragkb;"
psql -U $(whoami) -d ragkb -c "CREATE EXTENSION vector;"
# 重启后端，Flyway 会自动重建所有表
```

---

## 15. 扩展开发指南

### 15.1 如何添加新的文档格式支持

在 `DocumentService.java` 中，Apache Tika 已经自动支持大多数格式。如需手动扩展：

```java
// 在文件类型白名单中添加新类型
private static final Set<String> ALLOWED_TYPES = Set.of(
    "pdf", "txt", "md", "docx", "xlsx", "csv",
    "pptx"  // 新添加的格式
);
```

### 15.2 如何接入其他 AI 模型

实现 `AiProvider` 接口即可：

```java
@Component
public class MyNewAiProvider implements AiProvider {
    @Override
    public String getMode() { return "my-new-mode"; }

    @Override
    public String chat(String systemPrompt, String userMessage) {
        // 调用你的 AI 模型
    }

    @Override
    public float[] embed(String text) {
        // 返回向量
    }

    @Override
    public int getEmbeddingDimension() {
        return 1280;  // 你的模型维度
    }
}
```

然后在 `DynamicAiProvider` 中注册新模式即可。

### 15.3 如何调整 RAG 效果

编辑 `backend/src/main/resources/application.yml`：

```yaml
app:
  rag:
    chunk-size: 800          # 增大块大小 → 更多上下文，但可能稀释语义
    chunk-overlap: 150       # 增大重叠 → 减少信息断裂
    top-k: 8                 # 增大检索数量 → 更多参考资料
    similarity-threshold: 0.3 # 降低阈值 → 宽松匹配（召回更多但可能不相关）
    max-history-rounds: 5    # 增大历史轮数 → 更连贯，但占更多 token
```

### 15.4 如何切换对话模型

编辑 `application.yml`：

```yaml
spring:
  ai:
    ollama:
      chat:
        model: qwen2.5:14b    # 换成更大的模型（需要更多内存）
```

### 15.5 如何添加新功能模块

按照现有的分层架构即可：

1. 在 `model/entity/` 创建实体类
2. 在 `repository/` 创建数据访问接口
3. 在 `service/` 创建业务逻辑
4. 在 `controller/` 创建 REST API
5. 在 `frontend/src/api/` 创建前端 API 调用
6. 在 `frontend/src/views/` 创建前端页面
7. 在 `frontend/src/router/` 添加路由
8. 在 `SecurityConfig.java` 添加权限规则

---

## 附录：技术术语表

| 术语 | 通俗解释 |
|------|---------|
| RAG | 检索增强生成——AI 先查资料再回答，不是凭空编造 |
| LLM | 大语言模型——读了很多书、会说话的 AI 大脑 |
| Embedding | 向量嵌入——把文字变成一串数字，用来计算语义相似度 |
| Vector | 向量——一组有序的数字，用来表示文字在语义空间中的位置 |
| Token | AI 处理文本的最小单位，一个中文约等于 1.5 个 token |
| API | 程序之间的接口，前端和后端沟通的「语言」 |
| REST | 一种设计 API 的风格，用 URL 表示资源 |
| JWT | JSON Web Token——加密的电子身份证 |
| SSE | 服务器推送技术——让 AI 的回答像打字一样逐字显示 |
| ORM | 对象关系映射——用 Java 对象操作数据库，不用手写 SQL |
| CORS | 跨域资源共享——允许前端从不同端口访问后端 |
| pgvector | PostgreSQL 的向量扩展——让数据库能搜索「相似」的内容 |
| Flyway | 数据库迁移工具——自动创建和更新数据库表结构 |
| Maven | Java 项目构建工具——管理依赖、编译、打包 |
| Vite | 前端构建工具——快速编译、热更新 |
| Pinia | Vue 的状态管理库——在多个组件间共享数据 |

---

> 📝 文档版本：v1.0 | 最后更新：2026-07-04
